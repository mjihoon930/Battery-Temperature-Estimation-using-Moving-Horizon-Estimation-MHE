function hFig = fd()
    % createDockedFigure - Creates a new docked figure window
    %
    % Output:
    %   hFig - Handle to the created figure

    % Create the figure
    hFig = figure('WindowStyle', 'docked');

    % Optionally, you can customize the figure here
    % For example, set default axes, title, etc.
end
